# FastAPI and server (updated for Python 3.13 support)
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# Pydantic and settings (updated for Python 3.13 support)
pydantic==2.10.3
pydantic-settings==2.6.1

# Git operations
gitpython==3.1.40

# HTTP requests (for Git PR creation)
requests==2.31.0

# Spark
pyspark==3.5.0
delta-spark==3.0.0

# Data processing (required by connections module)
pandas==2.1.4

# ML dependencies (required by ml module)
mlflow==2.9.2
scikit-learn==1.3.2

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Configuration
pyyaml==6.0.1

# JSON Schema validation (Phase 2)
jsonschema==4.23.0

# Development
python-dotenv==1.0.0
